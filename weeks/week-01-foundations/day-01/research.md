# Research Questions
1. Why does Bash execute external commands instead of implementing functionality internally?
2. What would break if Bash tried to understand programs instead of launching them?
3. Why is text used as the interface between programs and UNIX?
4. What role does the kernel play that Bash cannot?
